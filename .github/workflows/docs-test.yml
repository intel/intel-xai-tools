name: Build and Test docs
on:
  pull_request:
    types: [submitted]
  # run the workflow if changes pushed to main or release branches
  push:
    branches: '**'
    paths: '**'

# installs dependencies, build the docs and push it to `gh-pages`
jobs:
  docs-test:
    runs-on: [ aia-devops ]
    container:
      image: amr-registry.caas.intel.com/aiops/tlt-dev
      env:
        http_proxy: ${{ secrets.HTTP_PROXY }}
        https_proxy: ${{ secrets.HTTPS_PROXY }}
        no_proxy: ${{ secrets.NO_PROXY }}
      credentials:
        username: ${{ secrets.REGISTRY_USER }}
        password: ${{ secrets.REGISTRY_TOKEN }}
      volumes:
        - /tf_dataset/dataset/transfer_learning:/tmp/data
    steps:
    - uses: actions/checkout@v3
    - name: Install virtualenv
      run: pip install virtualenv
    - name: Install model-card-gen dependency
      run: cd model_card_gen && pip install .
    - name: Build explainer whl
      run: cd explainer && make explainer-whl explainer-install
    - name: Install explainer dependency
      run: cd explainer && make explainer-whl explainer-install
    - name: Install feature_attributions_explainer
      run: explainer update feature_attributions_explainer
    - name: Install metrics_explainer
      run: explainer update metrics_explainer
    - name: Install lm_layers_explainer
      run: explainer update lm_layers_explainer
    - name: Install docs dependencies
      run: cd docs && make install
    # make documentation
    - name: Make documentation
      run: cd docs && make html
